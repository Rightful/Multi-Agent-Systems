use "ut-environment.jar" as environment with
	visualizer = "rmi://127.0.0.1:1099",
	botServer = "ut://127.0.0.1:3000",
	controlServer = "ut://127.0.0.1:3001",
	logLevel = "WARNING", 
	bots = [
			[
			 	["name", "Attacker 1"],
				["skill", 5],
				["skin", "BotA"],
				["logLevel", "WARNING"],
				["team", "blue"]
			]
			,
			[
			 	["name", "Attacker 2"],
				["skill", 5],
				["skin", "BotA"],
				["logLevel", "WARNING"],
				["team", "blue"]
			],
			
			[
			 	["name", "Defender 1"],
				["skill", 5],
				["skin", "BotA"],
				["logLevel", "WARNING"],
				["team", "blue"]
			],
			[
			 	["name", "Defender 2"],
				["skill", 5],
				["skin", "BotA"],
				["logLevel", "WARNING"],
				["team", "blue"]
			]
		]
		.

define defencebot as agent{
	use defenceMain as main.
	use defenceInit as init.
	use defenceEvents as event.
}

define attackbot as agent{
	use attackMain as main.
	use attackInit as init.
	use attackEvents as event.
}
define GOD as agent{
	use GODMain as main.
	use GODInit as init.
	use GODEvent as event.
}
define UTBot as agent{
	use UTBotInit as init.
	use UTBotMain as main.
	
}
launchpolicy{
		%when type=bot launch attackbot with max = 1.
		%when type=bot launch defencebot with max = 2.
		when type=bot launch UTBot with max =4 .
		when type=bot launch GOD with max=1.
}