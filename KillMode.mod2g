use UTbot as knowledge.
use Action as actionspec.
exit = never.
order = linear.
module KillMode {
	 if bel(holdingFlag) then exit-module.
	 if bel(enemyBot(ID,Position)) then navigate(ID).
	 if bel(fraggedBot(ID), self(ID,_,Team)) then exit-module.
	 if bel(not(enemyBot(_,_))) then exit-module.  
}