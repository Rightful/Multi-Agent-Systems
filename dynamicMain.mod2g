use UTbot as knowledge.

use attackMain as module.
use defenceMain as module.

module dynamicMain {
	
	if bel(enemyBaseLocation(location(X,Y,Z)), ourBaseLocation(location(Xi,Yi,Zi)), DX is abs(X - Xi), DY is abs(Y - Yi), D is sqrt(DX * DX + DY * DY), D < 8577) then attackMain + exit-module.
	if bel(enemyBaseLocation(location(X,Y,Z)), ourBaseLocation(location(Xi,Yi,Zi)), DX is abs(X - Xi), DY is abs(Y - Yi), D is sqrt(DX * DX + DY * DY), D > 8577) then defenceMain + exit-module.

}