use UTbot as knowledge.
use Action as actionspec.
exit = always.
order = linearrandom.
module CaptureTheFlag {
		if a-goal(at(ID))then navigate(ID).
		%if a-goal(killModus), bel(navPoint(ID,_,_))then navigate(ID).
}